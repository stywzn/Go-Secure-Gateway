# Binaries (anchored to repo root so they don't match cmd/gateway, cmd/mockbackend source dirs)
bin/
/gateway
/mockbackend
*.exe

# Test / coverage output
coverage.out
*.test

# Editor / OS
.idea/
.vscode/
.DS_Store

# Local overrides
*.local.yaml

# Python (e2e-py test framework)
.venv/
__pycache__/
*.pyc
.pytest_cache/
allure-results/
allure-report/
