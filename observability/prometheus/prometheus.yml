global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: gateway
    static_configs:
      - targets: ["gateway:8080"]
    # The gateway exposes Prometheus metrics on /metrics (public, no auth).
